# Internal Docs

Bootstrap and planning artifacts. Useful as historical context, not as ship-facing user docs.

- `implementation-plan.md` — phased plan that produced the current harness shape.
- `phase-1.5-fidelity.md` — fidelity-oracle runbook against the legacy
  current-schema openpi path; still valid if you need to re-run the oracle.
- `phase-2-runtime-spikes.md` — bootstrap-only runtime spike runbook.
- `pain/` — observed pain notes from the current-schema bootstrap.
- `spikes/` — current-schema gap matrix and probe artifacts.
