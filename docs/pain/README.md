# Observed Pain Reports

This directory is reserved for post-integration pain reports.

The first real report should be written only after:

- `OpenPI + DK-1` current-schema integration exists
- phase-1.5 preprocessing parity is wired and passing
- phase-1.5 action parity is wired and passing with configured tolerances
- phase-1.5 negative control proves the parity battery can detect a wrong path
- a `GR00T` spike exists
- a `MolmoAct2` spike exists

That report should drive the first internal representation design.
